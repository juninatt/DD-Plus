server:
  port: 8081

spring:
  config:
    import:
      - optional:application-telegram.yml
      - optional:application-finnhub.yml
      - optional:application-marketaux.yml
      - optional:application-subscription.yml
  main:
    web-application-type: none
